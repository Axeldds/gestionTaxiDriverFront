import { SortparamsDirective } from './sortparams.directive';

describe('SortparamsDirective', () => {
  it('should create an instance', () => {
    const directive = new SortparamsDirective();
    expect(directive).toBeTruthy();
  });
});
